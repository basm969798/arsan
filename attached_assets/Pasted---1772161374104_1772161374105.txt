"لدينا مهمتان حرجتان في مشروع 'أرصن'. قم بتنفيذهما بالترتيب التالي بدقة متناهية ولا تتخطى أي خطوة:

أولاً: إصلاح خطأ الواجهة الأمامية (TypeError في Dashboard)
قم بتنفيذ هذا السكريبت في الـ Terminal لتحديث ملف لوحة التحكم وجعله محصناً ضد أخطاء البيانات:

Bash
cd frontend
cat << 'EOF' > src/app/page.tsx
'use client';
import { useEffect, useState } from 'react';
import { apiClient } from '@/lib/api-client';
import { TrendingUp, Package, FolderTree, Activity, ArrowRight, Zap } from 'lucide-react';
import Link from 'next/link';
export default function DashboardPage() {
const [companyId, setCompanyId] = useState('');
const [stats, setStats] = useState({ activeOrdersCount: 0, catalogCount: 0 });
const [loading, setLoading] = useState(true);

useEffect(() => {
const userInfo = localStorage.getItem('user_info');
if (userInfo) setCompanyId(JSON.parse(userInfo).companyId);

const fetchStats = async () => {
  try {
    const [ordersRes, catalogRes] = await Promise.all([
      apiClient.get('/orders').catch(() => ({ data: [] })),
      apiClient.get('/catalog').catch(() => ({ data: [] }))
    ]);
    
    const ordersData = Array.isArray(ordersRes.data) ? ordersRes.data : [];
    const catalogData = Array.isArray(catalogRes.data) ? catalogRes.data : [];
    const activeOrders = ordersData.filter(
      (order: any) => order && !['COMPLETED', 'REJECTED', 'CANCELLED'].includes(order.status)
    );
    setStats({ activeOrdersCount: activeOrders.length, catalogCount: catalogData.length });
  } catch (error) {
    console.error('Failed to load stats', error);
  } finally {
    setLoading(false);
  }
};
fetchStats();
}, []);

return (
<div style={{ padding: '2rem', maxWidth: '1100px', margin: '0 auto' }}>
<div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: '2.5rem' }}>


### ثانياً: إصلاح الخلل الجذري في تسجيل الدخول (Backend Auth)
الحساب owner_827@arson.com بكلمة مرور Arson@2026 لا يستطيع الدخول. المشكلة تكمن في فشل عملية المطابقة (Validation). افعل الآتي:

إصلاح AuthService: >    * افتح ملف backend/src/auth/auth.service.ts.

  * تأكد من أن مقارنة كلمة المرور تتم حصراً باستخدام `await bcrypt.compare(password, user.password)`. (استخدم `bcryptjs` إذا كانت هي المثبتة). امسح أي مقارنة نصية عادية.
إضافة كشافات التتبع (Debug Logs):

  * أضف هذه الأسطر داخل دالة التحقق (`validateUser` أو `login`) وقت المحاولة:
    `console.log('Login attempt for:', email);`
    `console.log('User found in DB:', !!user);`
    `console.log('Password match result:', isMatch);`
تأكيد التشفير في قاعدة البيانات (TypeORM Seeding):

  * أنشئ أو حدّث الحساب `owner_827@arson.com` في قاعدة البيانات برمجياً لتتأكد أن حقل `password` يحتوي على الـ Hash الصحيح الخاص بكلمة `Arson@2026` باستخدام `bcrypt.hash`.
فحص الـ API Client:

  * تأكد أن `frontend/src/lib/api-client.ts` يرسل الـ Payload (البريد والباسورد) بشكل صحيح كـ JSON إلى الرابط السحابي.
أخبرني بمجرد الانتهاء: ما هو السطر الذي كان يسبب المشكلة في تسجيل الدخول؟ وهل الواجهة الأمامية تعمل الآن بدون أخطاء؟