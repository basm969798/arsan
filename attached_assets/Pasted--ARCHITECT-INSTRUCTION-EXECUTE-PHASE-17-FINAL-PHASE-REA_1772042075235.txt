# ARCHITECT INSTRUCTION: EXECUTE PHASE 17 (FINAL PHASE - REAL-TIME NOTIFICATIONS)
You are a strict execution agent operating in ULTRA MODE. 
Your goal is to implement WebSockets and Persistent Notifications. Do NOT break compilation and MUST include the join-room logic.

STEP 1: Execute the following bash script to implement the Real-time infrastructure safely:

```bash
cd backend

# 1. Install WebSocket dependencies
npm install @nestjs/websockets @nestjs/platform-socket.io socket.io

# 2. Create Notification Entity
cat << 'EOF' > src/modules/notifications/domain/entities/notification.entity.ts
import { Entity, Column, Index } from 'typeorm';
import { BaseEntity } from '../../../../common/database/base.entity';

@Entity('notifications')
export class Notification extends BaseEntity {
  @Index()
  @Column({ type: 'uuid' })
  companyId: string; // Law 9 isolation

  @Column()
  message: string;

  @Column({ default: false })
  isRead: boolean;
}
EOF

# 3. Create WebSocket Gateway (Fixed: Added Join Room Logic)
mkdir -p src/modules/notifications/infrastructure/gateways
cat << 'EOF' > src/modules/notifications/infrastructure/gateways/notification.gateway.ts
import { WebSocketGateway, WebSocketServer, OnGatewayConnection, SubscribeMessage, MessageBody, ConnectedSocket } from '@nestjs/websockets';
import { Server, Socket } from 'socket.io';

@WebSocketGateway({ cors: { origin: '*' } })
export class NotificationGateway implements OnGatewayConnection {
  @WebSocketServer()
  server: Server;

  handleConnection(client: Socket) {
    console.log(`[NotificationGateway] 🔌 Client connected: ${client.id}`);
  }

  // CRITICAL FIX: Frontend must call this to join their specific company room
  @SubscribeMessage('joinCompany')
  handleJoinCompany(@MessageBody() data: { companyId: string }, @ConnectedSocket() client: Socket) {
    client.join(data.companyId);
    console.log(`[NotificationGateway] 🏢 Client ${client.id} joined company room: ${data.companyId}`);
    return { event: 'joined', data: data.companyId };
  }

  sendToCompany(companyId: string, message: string) {
    // Law 9: Emitting ONLY to the specific company room
    this.server.to(companyId).emit('notification', { message, timestamp: new Date() });
    console.log(`[NotificationGateway] 📤 Push sent to company: ${companyId}`);
  }
}
EOF

# 4. Update Notifications Service (Persistence + Push)
cat << 'EOF' > src/modules/notifications/application/services/notifications.service.ts
import { Injectable } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { Notification } from '../../domain/entities/notification.entity';
import { NotificationGateway } from '../../infrastructure/gateways/notification.gateway';

@Injectable()
export class NotificationsService {
  constructor(
    @InjectRepository(Notification)
    private readonly notificationRepo: Repository<Notification>,
    private readonly gateway: NotificationGateway,
  ) {}

  async send(companyId: string, message: string): Promise<void> {
    const notification = this.notificationRepo.create({ companyId, message });
    await this.notificationRepo.save(notification);

    this.gateway.sendToCompany(companyId, message);
  }
}
EOF

# 5. Wire Notifications Module
cat << 'EOF' > src/modules/notifications/notifications.module.ts
import { Module, Global } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';
import { Notification } from './domain/entities/notification.entity';
import { NotificationsService } from './application/services/notifications.service';
import { NotificationGateway } from './infrastructure/gateways/notification.gateway';

@Global()
@Module({
  imports: [TypeOrmModule.forFeature([Notification])],
  providers: [NotificationsService, NotificationGateway],
  exports: [NotificationsService],
})
export class NotificationsModule {}
EOF

# 6. Update PHASE.lock
echo "PHASE_17_REALTIME_NOTIFICATIONS_COMPLETE" > ../PHASE.lock
echo "PROJECT_BACKEND_CORE_COMPLETED" >> ../PHASE.lock