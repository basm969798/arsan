# ARCHITECT INSTRUCTION: EXECUTE PHASE 4 (ORDER ENGINE & CORE ENTITIES SCAFFOLD)
You are a strict execution agent operating in ULTRA MODE. Your goal is to execute PHASE 4 exactly as instructed.
Do NOT write business logic. Do NOT implement CQRS or Saga logic yet. Create ONLY the skeletons and structures requested.

STEP 1: Execute the following bash script in the terminal to create the domain entities and enums.

```bash
cd backend

# 1. Ensure directories exist
mkdir -p src/modules/companies/domain/entities
mkdir -p src/modules/users/domain/entities
mkdir -p src/modules/orders/domain/entities
mkdir -p src/modules/orders/domain/enums
mkdir -p src/modules/orders/domain/interfaces
mkdir -p src/modules/orders/application/services
mkdir -p src/modules/orders/api/controllers

# 2. Create Company Entity Skeleton
cat << 'EOF' > src/modules/companies/domain/entities/company.entity.ts
import { Entity, Column } from 'typeorm';
import { BaseEntity } from '../../../../common/database/base.entity';

@Entity('companies')
export class Company extends BaseEntity {
  @Column({ type: 'varchar', length: 255 })
  name: string;

  @Column({ type: 'boolean', default: true })
  isActive: boolean;
}
EOF

# 3. Create User Entity Skeleton
cat << 'EOF' > src/modules/users/domain/entities/user.entity.ts
import { Entity, Column, Index } from 'typeorm';
import { BaseEntity } from '../../../../common/database/base.entity';

@Entity('users')
export class User extends BaseEntity {
  @Index('IDX_USER_COMPANY')
  @Column({ type: 'uuid' })
  companyId: string;

  @Column({ type: 'varchar', length: 255, unique: true })
  email: string;

  @Column({ type: 'varchar', length: 255 })
  passwordHash: string;
}
EOF

# 4. Create Order Status Enum
cat << 'EOF' > src/modules/orders/domain/enums/order-status.enum.ts
export enum OrderStatus {
  NEW = 'NEW',
  PENDING_SUPPLIER = 'PENDING_SUPPLIER',
  ACCEPTED = 'ACCEPTED',
  PROCESSING = 'PROCESSING',
  READY_FOR_PICKUP = 'READY_FOR_PICKUP',
  COMPLETED = 'COMPLETED',
  REJECTED = 'REJECTED',
  CANCELLED = 'CANCELLED'
}
EOF

# 5. Create Order Entity Skeleton (Compliant with Law 8: Optimistic Concurrency & Law 9: Tenant Isolation)
cat << 'EOF' > src/modules/orders/domain/entities/order.entity.ts
import { Entity, Column, Index, VersionColumn } from 'typeorm';
import { BaseEntity } from '../../../../common/database/base.entity';
import { OrderStatus } from '../enums/order-status.enum';

@Entity('orders')
export class Order extends BaseEntity {
  @Index('IDX_ORDER_COMPANY')
  @Column({ type: 'uuid' })
  companyId: string;

  @Column({ type: 'uuid' })
  createdById: string;

  @Column({ type: 'enum', enum: OrderStatus, default: OrderStatus.NEW })
  status: OrderStatus;

  @VersionColumn()
  version: number; // Law 8: Optimistic Concurrency
}
EOF

# 6. Create Order Event Interface (Compliant with Law 10: Events First)
cat << 'EOF' > src/modules/orders/domain/interfaces/order-event.interface.ts
export interface IDomainEvent {
  eventId: string;
  aggregateId: string;
  eventType: string;
  timestamp: Date;
  version: number;
  payload: any;
}
EOF

# 7. Create Order Controller & Service Shells
cat << 'EOF' > src/modules/orders/application/services/orders.service.ts
import { Injectable } from '@nestjs/common';

@Injectable()
export class OrdersService {
  // Scaffold only. Logic will be added in later phases.
}
EOF

cat << 'EOF' > src/modules/orders/api/controllers/orders.controller.ts
import { Controller } from '@nestjs/common';

@Controller('orders')
export class OrdersController {
  // Scaffold only.
}
EOF

# 8. Update PHASE.lock
echo "PHASE_4_ORDER_ENGINE_SCAFFOLD_COMPLETE" > ../PHASE.lock