Act as a Senior System Architect and Expert Backend Developer. Your task is to audit the current codebase and implement missing features based strictly on the provided "SYSTEM_INVARIANTS.md" document. 

Our goal is to execute this with MINIMAL REWRITE COST. We want to verify what is currently built, fix what violates the invariants, and build ONLY what is missing.

Here is your execution plan. You must strictly follow these phases:

### PHASE 1: READ-ONLY AUDIT (Do not write or modify code yet)
Scan the entire existing codebase (Models, Controllers, Routes, Services, DB schemas/logic) and evaluate it against the invariants provided below. Pay special attention to:
1. Multi-Tenancy: Is `company_id` present and enforced everywhere?
2. Event & Financial Immutability: Are there any `UPDATE` or `DELETE` operations on events or financial records?
3. State Machine: Are transitions validated, or is it direct mutation?
4. Database Layer: How is the current DB handling these rules? 

### PHASE 2: GAP ANALYSIS REPORT
Generate a concise report outlining:
- 🟢 COMPLIANT: What current code aligns with the invariants.
- 🔴 VIOLATIONS: What current code directly violates the invariants (e.g., direct state mutations, missing tenant isolation).
- 🟡 MISSING: What components described in the invariants are completely missing.

### PHASE 3: COST-EFFECTIVE IMPLEMENTATION PLAN
Wait for my approval on the report. Once approved, you will propose a step-by-step implementation plan to fix the violations and build the missing parts. 
Constraint: Prioritize adding middleware, validation wrappers, and event-bus patterns over completely rewriting existing business logic, to keep the cost and risk low.

--- 
START OF SYSTEM_INVARIANTS.md
---
NON-NEGOTIABLE ARCHITECTURAL INVARIANTS

0. PURPOSE & AUTHORITY
This document defines the absolute architectural invariants of the system.
These invariants:
- Apply to all services, modules, agents, jobs, APIs, and integrations.
- Override any conflicting implementation detail.
- Are mandatory for both human and AI-generated implementations.
If implementation conflicts with this document:
THIS DOCUMENT PREVAILS.

1. SYSTEM DEFINITIONS
Tenant = A company (isolated business boundary).
Aggregate = A transactional consistency boundary.
Event = An immutable record describing something that happened.
Financial Event = An immutable ledger entry affecting monetary state.
Derived State = Any state computed from events or validated transitions.

2. MULTI-TENANCY INVARIANTS
2.1 Tenant Isolation
- Every tenant-owned domain table MUST contain company_id.
- All queries MUST be filtered by company_id.
- Cross-tenant data access is strictly forbidden.
- No shared mutable state between tenants.
- Caches MUST be tenant-aware.
2.2 Database Enforcement
- company_id MUST be indexed.
- Composite indexes MUST include company_id where applicable.
- Foreign keys MUST NOT allow cross-tenant references.

3. AGGREGATE BOUNDARY INVARIANTS
- Aggregates MUST NOT directly mutate other aggregates.
- Cross-aggregate coordination MUST occur ONLY via Domain events or Process Manager orchestration.
- All aggregate mutations MUST occur via domain commands.

4. EVENT INTEGRITY INVARIANTS
4.1 Event Immutability
- All domain events are append-only. UPDATE and DELETE are forbidden.
- Corrections MUST use compensating events.
4.2 Event Ordering & Concurrency
- Events MUST maintain strict ordering per aggregate.
4.3 State Derivation
- Domain state MUST be derived from Events OR Validated state transitions that emit events.
- Direct mutation bypassing event logic is forbidden.

5. FINANCIAL INTEGRITY INVARIANTS
5.1 Financial Immutability
- financial_events table is append-only. UPDATE and DELETE are forbidden.
5.2 Price Locking
- Order price MUST be locked at ACCEPTED state. Locked price MUST NEVER change.
5.3 Ledger Consistency
- Every financial event MUST include: company_id, reference_id, event_type, amount, currency, timestamp.
- Direct mutation of balance fields is forbidden.

6. ORDER LIFECYCLE INVARIANTS
6.1 State Machine Enforcement
- Order states MUST follow SYSTEM_STATE_MACHINE_MATRIX.md. Skipping states is forbidden.
6.2 State Change Rule
- Every state change MUST emit an event. Direct status mutation is forbidden.

7. API CONTRACT INVARIANTS
- API MUST NOT mutate state outside defined commands.

8. AUTHORIZATION INVARIANTS
- Permission checks MUST NOT be bypassable.
- Tenant validation MUST occur BEFORE permission evaluation.

9. DATA INTEGRITY INVARIANTS
- Hard deletes on event or financial tables are strictly forbidden.

10. MUTATION & CONSISTENCY RULES
The system strictly forbids: Direct mutation of derived state, financial totals, or implicit state changes.

11. OBSERVABILITY & AUDIT INVARIANTS
- Every critical domain action MUST be traceable. Silent failures are forbidden.

12. AGENT EXECUTION RULES
MUST NOT: Invent new states, Modify immutable records, Remove tenant isolation, Bypass event emission.
---
END OF SYSTEM_INVARIANTS.md
---

Acknowledge these instructions and begin PHASE 1 immediately. Let me know when the PHASE 2 report is ready.