{
  "name": "arsan",
  "private": true,
  "scripts": {
    "setup": "bash setup.sh",
    "dev": "cd backend && npm run start:dev",
    "build": "docker-compose build",
    "up": "docker-compose up -d"
  }
}
