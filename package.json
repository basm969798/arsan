{
  "name": "arsan",
  "private": true,
  "scripts": {
    "setup": "bash setup.sh",
    "dev": "bash setup.sh",
    "build": "docker-compose build",
    "up": "docker-compose up -d"
  },
  "dependencies": {
    "@nestjs/jwt": "^11.0.2",
    "@nestjs/passport": "^11.0.5",
    "bcrypt": "^6.0.0",
    "passport": "^0.7.0",
    "passport-jwt": "^4.0.1"
  }
}
